import React from "react";

import Headline from "../Headline";

const AuthOptions = () => {
  return (
    <div>
      <Headline
        headline="Login in/Register"
        subline="Get ready to access amazing content"
        as="h2"
      />
    </div>
  );
};

export default AuthOptions;
